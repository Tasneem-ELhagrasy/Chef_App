import 'package:flutter/material.dart';

class AppIcons {
  static const Icon passwordShow = Icon(Icons.visibility_outlined);
  static const Icon passwordHide = Icon(Icons.visibility_off_outlined);
  static const Icon back = Icon(Icons.keyboard_backspace);
}
